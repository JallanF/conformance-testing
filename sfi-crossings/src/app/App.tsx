// =============================================================================
// WHAT: Application root
// ROLE: Hosts the provider hierarchy (Theme → Tooltip → Query → Auth → Router)
// NOTE: Providers are added here as the features that need them land — see
//       freight-ref-arch-v11/src/app/App.tsx for the target shape.
// =============================================================================

export function App() {
  return null
}
